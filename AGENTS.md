# AGENTS.md

本文件面向在本仓库中工作的 AI 编码 agent（Claude Code、Cursor、Copilot 等）。所有 agent 在修改本仓库前必须先读取并遵守本文件。

## 项目性质：这是一个公开的开源仓库

`filixpay-saleor` 是 FilixPay 面向 Saleor Commerce 的开源支付集成（Payment App），采用 Apache-2.0 协议公开托管在 GitHub 上，**任何人都可以克隆、fork、查看完整提交历史**。据此，agent 在这里的行为标准要严于一般私有项目：

- 默认假设你写入仓库的任何内容都会被公开、被抓取、被永久保留（即使后续删除，历史记录里仍可能留存）。
- 不要把内部专属信息（真实域名以外的内网地址、真实的商户/用户/支付数据、公司内部讨论、未公开的产品计划等）写进代码、注释、commit message 或文档里。

## 规则 1：禁止在测试中使用真实或敏感的个人数据

编写或修改任何测试（`*.test.ts`、`*.test.tsx`、fixtures、mock 数据等）时：

- **一律使用明显虚构的数据**，不要使用任何可能真实存在的邮箱、手机号、身份证号、银行账号、卡号、姓名等。
  - 邮箱：使用 `xxx@example.com` / `xxx@example.org`。
  - 手机号（中国大陆）：使用官方文档保留号段，如 `13800138000`。
  - 身份证 / 统一社会信用代码 / 银行卡号：使用明显不合法或官方示例格式的号码（例如全零、递增数字、带有 `TEST`/`FAKE` 标记），不要编造出格式上以假乱真、可能与真人重号的号码。
- 不要从真实的生产数据、日志、聊天记录、截图中复制样例值到测试或 mock 里，哪怕看起来是"随手编的"。
- 如果在改动过程中发现已有代码/测试里存在疑似真实的个人数据，停下来在 PR 描述或对话中标出来，不要自行"顺手"提交修改掉，交由人工确认后再处理（可能涉及需要清理 git 历史）。

## 规则 2：禁止 agent 自动创建或提交单元测试文件

- Agent 不得主动创建、生成或提交 `*.test.ts`、`*.test.tsx`、`__tests__/**` 等测试文件，也不得在未明确被要求的情况下修改已有测试文件的内容。
- 如果任务确实需要测试覆盖，在回复中说明"建议补充哪些测试"，把测试代码留给人工编写/审核后再提交，不要自己直接写入仓库。
- 已存在的测试文件可以阅读、运行（`npm run test` / `vitest run` / `pnpm test`）以验证改动没有破坏现有行为，但不要修改其内容或新增测试用例。

## 其他基本要求

- 不要提交任何形式的密钥、token、密码、`.env`、`.env.local`、Saleor auth 文件（如 `.saleor-app-auth.json`、`.auth-data.json`）、私钥文件——只允许修改 `.env.example` 这类占位符文件。
- 不要引入生产环境的真实域名、IP、内网地址作为默认值；所有环境相关配置必须通过环境变量传入（参考现有 `.env.example` 的写法）。
- Commit message 保持清晰、面向公开读者，不要在其中泄露内部信息。
- 本仓库是 **Payment App**（支付），不是商品目录同步服务：不要把 catalog / `storefrontUrl` / `MANAGE_PRODUCTS` 同步逻辑塞进本项目。
